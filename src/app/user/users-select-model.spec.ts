import { UsersSelectModel } from './users-select-model';

describe('UsersSelectModel', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new UsersSelectModel()).toBeTruthy();
  });
});
